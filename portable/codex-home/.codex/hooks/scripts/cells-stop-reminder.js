#!/usr/bin/env node

process.stdout.write(JSON.stringify({
  systemMessage: 'If this was Cells work, confirm verify evidence, source decisions, command policy, and residual risks are reported before closing.'
}));
