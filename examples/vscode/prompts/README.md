# Phase prompts

These short prompts select a Cells intent and inherit the native orchestrator tools. They do not override catalog access or force a full phase chain for a scoped request. Source files are generated from `scripts/generate_adapters.py`.
