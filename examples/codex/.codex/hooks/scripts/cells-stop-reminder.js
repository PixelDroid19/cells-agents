#!/usr/bin/env node

process.stdout.write(JSON.stringify({
  systemMessage: 'Before closing Cells work, confirm verify evidence, source decisions, command policy, and residual risks are reported.'
}));
